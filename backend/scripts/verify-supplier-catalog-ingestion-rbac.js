#!/usr/bin/env node
"use strict";const assert=require("assert"),fs=require("fs"),path=require("path"),{PERMISSIONS,ROLES,getPermissionsForRole}=require("../services/adminAuthorizationService");let checks=0;const ok=(x,m)=>(assert(x,m),checks++);ok(PERMISSIONS.SUPPLIER_CATALOG_INGEST==="SUPPLIER_CATALOG_INGEST","permission exists");ok(getPermissionsForRole(ROLES.OWNER).includes(PERMISSIONS.SUPPLIER_CATALOG_INGEST),"OWNER allowed");for(const role of [ROLES.OPERATIONS,ROLES.FINANCE,ROLES.SUPPORT,ROLES.CATALOG])ok(!getPermissionsForRole(role).includes(PERMISSIONS.SUPPLIER_CATALOG_INGEST),`${role} denied`);const source=fs.readFileSync(path.resolve(__dirname,"../routes/supplier.js"),"utf8"),route=source.indexOf('router.post("/admin/supplier-catalog/automation/:supplierCode/run"');ok(route>=0,"manual route exists");ok(source.indexOf("requireAdminPermission(PERMISSIONS.SUPPLIER_CATALOG_INGEST)",route)>route,"manual route permission guarded");console.log(JSON.stringify({result:"PASS",checks,owner:true,operations:false,finance:false,support:false,catalog:false,productionWrites:0},null,2));
