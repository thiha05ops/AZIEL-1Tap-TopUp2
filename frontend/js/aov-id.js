// frontend/js/aov-id.js
// Thin Arena of Valor (ID) page configuration for the shared AZIEL game flow.

window.AZIEL_GAME_FLOW?.init({
    game: "Arena of Valor (ID)",
    gameKey: "aovid",
    userIdSelector: "#userId",
    zoneIdSelector: "",
    zoneRequired: false,
    pendingReturnUrl: "aov-id.html"
});
